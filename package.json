{
  "name": "hnpwa-7.0.0",
  "version": "1.0.0",
  "scripts": {
    "dev": "dojo build --mode dev --watch --serve",
    "build": "dojo build --mode dist",
    "build:dev": "dojo build --mode dev",
    "prettier": "prettier --write \"{docs,src,tests}/**/*.{ts,tsx,md,css}\"",
    "test": "dojo test",
    "test:unit": "dojo build --mode unit && dojo test --unit --config local",
    "test:functional": "dojo build --mode functional && dojo test --functional --config local",
    "test:all": "dojo build --mode unit && dojo build --mode functional && dojo test --all --config local"
  },
  "dependencies": {
    "@dojo/framework": "./dojo-framework-7.0.0-alpha.10.tgz",
    "tslib": "~1.9.1"
  },
  "devDependencies": {
    "@dojo/cli": "next",
    "@dojo/cli-build-app": "next",
    "@dojo/cli-test-intern": "next",
    "@types/node": "~9.6.5",
    "prettier": "^1.19.1",
    "typescript": "~3.4.5"
  },
  "prettier": {
    "singleQuote": true,
    "tabWidth": 4,
    "useTabs": true,
    "printWidth": 120,
    "arrowParens": "always"
  }
}
